<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColor</item>
        <item name="colorAccent">@color/primaryColor</item>
    </style>

    <style name="BGcolor">
        <item name="android:background">@color/primaryColor</item>
    </style>

    <style name="icons" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">white</item>
    </style>

    <style name="note1" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">gray</item>
    </style>

    <style name="note2" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">red</item>
    </style>

    <style name="note3" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">purple</item>
    </style>

    <style name="note4" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">yellow</item>
    </style>

    <style name="divider" parent="android:style/Theme">
        <item name="android:layout_height">2dp</item>
        <item name="android:background">#fffff</item>
    </style>


</resources>
